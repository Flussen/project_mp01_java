server:
    port: 8080

spring:
    datasource:
        url: jdbc:postgresql://localhost:5432/test
        username: modding
        password: modding
        driver-class-name: org.postgresql.Driver

    jpa:
        hibernate:
            ddl-auto: update
        open-in-view: false
        properties:
            hibernate.jdbc.time_zone: UTC

logging:
    level:
        root: INFO
        org.springframework.security: DEBUG
        org.hibarnate.SQL: DEBUG